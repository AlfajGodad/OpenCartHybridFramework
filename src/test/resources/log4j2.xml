<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    
    <!-- 1. Properties: paths and patterns -->
    <Properties>
        <Property name="log-pattern">%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="log-dir">./logs</Property>
        <Property name="app-name">OpenCart</Property>
    </Properties>

    <!-- 2. Appenders -->
    <Appenders>
        
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log-pattern}"/>
        </Console>

        <!-- Rolling File Appender - rotates daily and keeps 7 days -->
        <RollingFile name="RollingFile" fileName="${log-dir}/${app-name}.log"
                     filePattern="${log-dir}/${app-name}-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${log-pattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/> <!-- roll every day -->
                <SizeBasedTriggeringPolicy size="10 MB"/> <!-- or when file > 10MB -->
            </Policies>
            <DefaultRolloverStrategy max="7"/> <!-- keep last 7 files -->
        </RollingFile>

    </Appenders>

    <!-- 3. Loggers -->
    <Loggers>
        
        <!-- Root Logger - catches everything -->
        <Root level="INFO">
            <AppenderRef ref="Console"/> 
            <AppenderRef ref="RollingFile"/>
        </Root>

        <!-- Reduce Selenium noise -->
        <Logger name="org.openqa.selenium" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

    </Loggers>
</Configuration>